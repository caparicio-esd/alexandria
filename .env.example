# Copy to .env. Never committed: .gitignore keeps every .env but this template.
#
# The database credentials are deliberately absent from config/config.yaml — a
# password in the document would be a password in the repository. Both the node
# and docker compose read them from here.

ALEXANDRIA_DB_USER=alexandria
ALEXANDRIA_DB_PASSWORD=change-me
ALEXANDRIA_DB_NAME=alexandria
